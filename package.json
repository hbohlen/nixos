{
  "name": "nixos-config",
  "private": true,
  "version": "0.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "fmt": "npx prettier --write .",
    "fmt:check": "npx prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "prettier-plugin-nix": "https://github.com/nix-community/prettier-plugin-nix/archive/refs/heads/main.tar.gz"
  }
}
